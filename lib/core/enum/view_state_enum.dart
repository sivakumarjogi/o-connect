enum ViewState { Idle, Busy }

enum Participants { host, coHost, pinnedUser, user }

enum ShowFlagsOnDashBoard { resound, ticker, emoji }

enum WebinarTopFutures { callToAction, record, bgm, timer, screenShare, pushLink, initialAction, }

enum ShowFlasOnDashBoardAtTop { timer, bgm, recordScreen }

enum ShowFlagOnTopHeader { pushLink, callToAction, recordScreen, timer, bgm }

enum AppPermissions { home, mic, notifications, screenShare }

enum ChatMessageFlag {
  initial,
  reply,
  edit,
  delete,
}

enum ChatTabDataFlag { Group, Private, QA, Other }

enum WebinarDashBoardIcons { moreFeatureIcon, membersIcon, moreIcon, chatIcon, videoIcon, micIcon }

enum ShareFilesRadioButtons { all, speakers }
